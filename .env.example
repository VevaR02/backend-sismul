# Example environment variables for backend (DO NOT commit secrets)
# Copy to `.env` locally and fill values before running.
ENV=production
SECRET_KEY=
DATABASE_URL=
# Optional: change if you want to override Uvicorn port
PORT=8000
